# Kafka Quarkus Consumer App

Set the Kafka connection configuration in the `application.properties` file.

Run the Quarkus app

```
./mvnw clean quarkus:dev
```

Navigate to http://localhost:8080/humidity to see the aggregated data stats.
